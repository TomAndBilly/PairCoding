import React, {Component} from 'react';
class HitButton extends Component{

render(){
    return(
        <div>
            <button> Hit </button>
        </div>
        );
    }
}

export default HitButton;